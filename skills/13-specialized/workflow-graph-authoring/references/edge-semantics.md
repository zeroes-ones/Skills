# Edge Semantics

Edges are serial by default; parallel fan-out uses a start-check predecessor and a join node. An edge transfers the run-state payload; conditional edges carry the condition in the manifest.
