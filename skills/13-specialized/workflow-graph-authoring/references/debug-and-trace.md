# Debug and Trace

Export run traces to inspect node states, payload snapshots, and gate results per step. Replay a stale resume by clearing temp run-state; traces are the debugging surface for graphs.
