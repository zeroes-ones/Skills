# Verify-Node Guide

A verify node runs the check declared for the artifact and returns pass/fail. Failed verify routes to revise-iteration or escalate per the graph; pass advances. REVIEW_PASS_AT sets the pass threshold.
