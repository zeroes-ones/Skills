# Security-Audit Orchestration

Scope the audit surface first, then run the security persona with the threat model as input. Findings are severity-ranked; any critical finding blocks the pipeline until fixed and re-verified. Never let a persona guess at a security verdict - route to the security skill.
