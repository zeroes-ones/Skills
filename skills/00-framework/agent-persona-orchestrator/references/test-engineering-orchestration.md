# Test-Engineering Orchestration

After code changes pass review, dispatch the test persona to produce the test plan, execute the suite, and report coverage gaps. The gate is evidence-based: a merge requires passing tests plus a written coverage delta.
