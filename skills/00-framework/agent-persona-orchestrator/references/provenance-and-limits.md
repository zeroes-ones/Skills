# Provenance and Limits

All persona outputs are [COMPUTED]/[ESTIMATED]/[VERIFIED] tagged per the repo standard. Orchestrator results inherit the weakest tag of their inputs; a pipeline result is only as trustworthy as its least-verified stage.
