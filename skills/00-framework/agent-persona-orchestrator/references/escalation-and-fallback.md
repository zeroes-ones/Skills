# Escalation and Fallback

Escalate when: scope changes, a persona is unavailable, or a gate cannot reach a decision. Fallback: a generic agent executes the persona's contract with explicit scope limits rather than blocking the flow.
