# Mock-Data Sync — References

This skill's reference material was consolidated into `SKILL.md` when the skill was rewritten; the
detail lives in that file's sections rather than in separate reference documents. This index maps
each topic to the `SKILL.md` section that owns it.

| Topic | SKILL.md section |
|---|---|
| Full DETECT → VALIDATE → SYNC → VERIFY cycle | Core Workflow |
| Factory naming, `DeepPartial` overrides, single-source-of-truth rules | Best Practices |
| Local hooks, CI configuration, manual validation commands | Validation Pipeline |
| Adding a new mock factory | Adding a New Mock Factory |
| Checklist for a backend schema change | Backend Schema Change Checklist |
| OpenAPI generator pipeline and its future | OpenAPI Generator (Future) |
| Failing checks and how to recover | Error Recovery, Error Decoder |

**Parent skill:** [mock-data-sync](../SKILL.md) — load it for the rules, decision trees, and
verification checklist; this file is only a pointer index.
