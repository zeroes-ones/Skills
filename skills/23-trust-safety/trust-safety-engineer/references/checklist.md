# Production Checklist

<!-- MACHINE-EXECUTABLE: Every item has an exact CLI validation command and auto-fix path -->

| ID | Checklist Item | Validation Command | Auto-Fix |
|----|---------------|-------------------|----------|
| **TS1** | Account integrity: CAPTCHA, phone verification, and device fingerprinting deployed and tested at expected registration volume | `curl -s http://localhost:${PORT}/api/account-integrity/status \| jq '.captcha'` must be `true` AND `jq '.phone_verify'` must be `true` AND `jq '.device_fingerprint'` must be `true` | `npx init-account-integrity --captcha --phone-verify --device-fingerprint --load-test` |
| **TS2** | ATO detection model validated against historical ATO incidents with recall > 0.95 | `npx validate-ato-model --test-set historical_ato_incidents.csv` must return `recall >= 0.95` | `npx init-ato-detection --train-on historical_ato_incidents.csv --min-recall 0.95` |
| **TS3** | Session management: device fingerprint binding, token rotation on sensitive actions, 12-hour absolute timeout configured | `grep -cP "device.fingerprint\|token.rotation\|absolute.timeout.*12" session_config.yaml` must be `>= 3` | `npx init-session-management --device-binding --token-rotation --absolute-timeout-hours 12` |
| **TS4** | Abuse detection pipeline end-to-end: rule engine → ML classification → human review queue, all with monitoring dashboards | `curl -s http://localhost:${PORT}/api/detection/pipeline/status \| jq '.rule_engine'` must be `true` AND `jq '.ml_classifier'` must be `true` AND `jq '.human_review_queue'` must be `true` | `npx init-abuse-pipeline --rule-engine --ml-classifier --human-review --dashboards` |
| **TS5** | False positive rate measured and below 0.1% for all automated actions; shadow-mode validation completed for current model | `curl -s http://localhost:${PORT}/api/detection/fpr \| jq '.overall'` must be `<= 0.001` AND `jq '.shadow_validated'` must be `true` | `npx measure-fpr --all-models --target 0.001 --require-shadow-validation` |
| **TS6** | In-app reporting flow tested: report submission, triage queue, automated action, user notification, appeal submission | `npx test-reporting-flow --scenarios "misinfo,crisis,harassment,spam"` must return exit 0 for all scenarios | `npx init-reporting-flow --test-scenarios --auto-notify --appeal-link` |
| **TS7** | CSAM detection: PhotoDNA/Thorn integration verified, NCMEC reporting pipeline tested end-to-end | `curl -s http://localhost:${PORT}/api/csam/detection/status \| jq '.photodna_integrated'` must be `true` AND `jq '.ncmec_pipeline_tested'` must be `true` | `npx init-csam-detection --photodna --thorn --ncmec-pipeline --test-end-to-end` |
| **TS8** | Self-harm detection: crisis intervention flow tested, survivor speech protection rules validated with clinical advisors | `npx test-crisis-flow --scenario self_harm --verify "resource_surface,human_review,welfare_followup"` must return exit 0 AND `grep -rn "survivor.speech\|recovery.discussion" self_harm_rules.yaml` must match | `npx init-self-harm-detection --crisis-flow --survivor-speech-protection --clinical-validation` |
| **TS9** | Anti-bot: behavioral analysis, rate limiting, CAPTCHA strategies deployed; bot registration rate < 1% | `curl -s http://localhost:${PORT}/api/anti-bot/status \| jq '.bot_registration_rate'` must be `< 0.01` | `npx init-anti-bot --behavioral-analysis --rate-limiting --captcha --target-bot-rate 0.01` |
| **TS10** | Threat model for patient communities documented, reviewed with clinical and security stakeholders, mitigation controls implemented | `grep -cP "threat.actor\|attack.vector\|mitigation\|residual.risk" threat_model.md` must be `>= 4` AND `grep -cP "clinical.review\|security.review" threat_model.md` must be `>= 2` | `npx init-threat-model --domain patient_community --require-clinical-review --require-security-review` |
| **TS11** | Evidence preservation: content freeze, chain of custody, legal hold workflow tested with legal team | `npx test-evidence-preservation --scenario legal_hold` must return exit 0 AND `jq '.rfc3161_timestamp'` must be `true` AND `jq '.sha256_hash'` must be `true` | `npx init-evidence-preservation --rfc3161 --sha256 --chain-of-custody --test-with-legal` |
| **TS12** | Moderation tooling: automated flagging, bulk actions, review queues deployed; moderator safety program operational | `curl -s http://localhost:${PORT}/api/moderation/tooling/status \| jq '.auto_flag'` must be `true` AND `jq '.moderator_safety_program'` must be `true` | `npx init-moderation-tooling --auto-flag --bulk-actions --moderator-safety-program` |
| **TS13** | Multilingual coverage validated: keyword lists exist and tested for all supported languages; code-switching detection operational | `curl -s http://localhost:${PORT}/api/detection/languages \| jq '[.[] \| select(.keyword_list_tested == false)] \| length'` must be `0` | `npx init-multilingual-detection --languages "$(cat .supported_languages)" --native-speaker-lexicon --code-switching` |
| **TS14** | Incident response integration: trust and safety incidents have defined severity levels, notification paths, and response runbooks | `grep -cP "SEV1\|SEV2\|SEV3\|notification.path\|response.runbook" incident_response.yaml` must be `>= 4` | `npx init-incident-response --severity-levels "SEV1,SEV2,SEV3" --notification-paths --runbooks` |
