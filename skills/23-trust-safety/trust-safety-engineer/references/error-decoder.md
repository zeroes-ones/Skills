# Error Decoder

<!-- MACHINE-EXECUTABLE: First column is exact grep regex for console/log matching -->

| 🖥️ Console Match (grep regex) | Symptom | Root Cause | Fix | 🔄 Auto-Recovery Loop |
|---|---|---|---|---|
| `grep -cP "shadowban\|silent.ban\|stealth.ban" enforcement_log.json` → `count > 0` AND `grep -cP "appeal.*shadowban\|shadowban.*reversed\|false.positive.*shadowban" transparency_report.json` → `0` | 50,000 legitimate users silently shadowbanned over 5 months — classifier had 2% false positive rate at 500K classifications/month, no feedback loop detected errors | Shadowban system at 98% precision produced 10,000 false positives/month. No observability — shadowbanned users can't see their content is hidden, can't appeal, platform can't detect errors. Discovered only when prominent patient advocate noticed zero engagement | Add feedback loop: track engagement for all users — if previously active user drops to zero for 30 days, auto-escalate. Provide "break glass" appeal channel. Publish shadowban statistics in transparency reports. Require mandatory human review for users with >90 days history | **1.** Audit shadowbanned users: `npx audit-shadowbans --min-account-age 90d`. **2.** Review flagged accounts: `npx review-shadowbans --sample 500 --human-review`. **3.** Restore false positives: `npx bulk-restore --user-ids false_positive_shadowbans.csv`. **4.** Add monitoring: `npx add-engagement-monitor --alert-on zero-engagement-30d`. **5.** Update policy: publish shadowban volume, appeal rate, and false-positive discovery rate in next transparency report |
| `grep -cP "block.*IP\|IP.range.*block\|CIDR.*DROP" firewall_rules.yaml` → `count > 0` AND `grep -cP "geolocation.impact\|legitimate.user.impact\|country.breakdown" firewall_rules.yaml` → `0` | IP-range block deployed against botnet — 2,500 legitimate users from Nigeria and India lost access; platform lost 80% of Nigerian user base in 48 hours | Hosting provider IP ranges blocked without checking geographic distribution of legitimate users behind those IPs. Block message in English only. Emerging-market users couldn't understand why they were banned | Run geolocation impact analysis before every IP-based block. If >0.1% of any country's users affected, provide alternative access (phone verification). Provide ban messages in all supported languages | **1.** Assess impact: `npx assess-ip-block-impact --cidr "$BLOCKED_RANGE"` → query active users by country. **2.** If >0.1% of any country affected: `npx add-bypass-path --method phone_verification --countries "$AFFECTED"`. **3.** Translate messages: `npx translate-ban-message --languages all_supported`. **4.** Deploy bypass: `npx deploy-access-bypass --production`. **5.** Monitor: `npx monitor-access-bypass --alert-on rejection-rate > 0.05` |
| `grep -cP "classifier.*false.positive\|model.*flagged.*legitimate\|40%.*false" moderator_feedback.csv` → `rate > 0.30` AND `grep -cP "general.social.media\|Jigsaw\|HateXplain\|off.the.shelf" model_training.yaml` → matches > 0 | 40% false positive rate on abuse classifier in cancer patient community — model flagged "I want to die" (chemo frustration), "fuck cancer" (community bonding), "this is killing me" (side effects) | General-purpose classifier trained on Twitter/Reddit toxic content datasets deployed to health community without fine-tuning. Model had no health-context awareness or patient vernacular training data | Replace with health-context classifier. Build golden dataset of 10,000+ labeled examples from YOUR community. Include patient advocates in labeling. Target <0.1% false positive rate on health-context content | **1.** Audit false positives: `npx audit-classifier-false-positives --model current --sample 1000 --domain health`. **2.** Build golden dataset: `npx create-golden-dataset --community-id $(cat .community_id) --size 10000 --include-patient-advocates`. **3.** Train health-context model: `npx train-classifier --dataset golden_dataset.json --context health_community`. **4.** Validate: `npx validate-classifier --model health_context_v1 --target-fpr 0.001`. **5.** Deploy in shadow mode for 2 weeks before enforcement: `npx deploy-classifier --mode shadow --duration 14d` |
| `grep -cP "moderator.*resign\|moderator.*stress.leave\|moderator.*burnout\|moderator.*trauma" hr_log.csv` → `rate > 0.20` over 6 months AND `grep -cP "exposure.limit\|content.rotation\|counseling\|wellness" moderator_program.md` → `0` | 4 of 12 moderators resigned, 3 on stress leave, 5 with secondary trauma symptoms in 6 months — all reviewed CSAM and graphic content with no specialization, limits, or support | All moderators reviewed all content types including CSAM and self-harm. No exposure time limits. No content rotation. No psychological support. Replacement cost exceeded proper wellness program cost by 4x | Implement engineering controls: exposure time limits enforced in queue system, content blurring by default, grayscale mode for graphic content, mandatory breaks. Add psychological support. Create content specialization — rotate staff off graphic queues | **1.** Audit moderator health: `npx audit-moderator-wellness --team moderation --metrics "turnover,stress_leave,burnout_survey"`. **2.** Implement exposure limits: `npx set-exposure-limits --max-hours 2 --queue graphic --enforce-in-system`. **3.** Add content rotation: `npx enable-content-rotation --frequency weekly --queues "graphic,text,appeals"`. **4.** Add wellness program: `npx init-moderator-wellness --counseling --exit-pathways`. **5.** Monitor: `npx monitor-moderator-wellness --alert-on turnover-rate > 0.10` |
| `grep -cP "evidence.challenged\|evidence.excluded\|chain.of.custody.*failed" legal_cases.csv` → `count > 0` | Evidence of harassment campaign excluded in court — screenshots not original data, timestamps not NTP-synced, no access log between preservation and court submission | Evidence preserved as rendered HTML screenshots without cryptographic hashes. Server timestamps without NTP synchronization proof. No chain-of-custody logging. Defense successfully challenged admissibility | Upgrade to forensic standards: NTP-synchronized timestamps (RFC 3161), SHA-256 hashing of all evidence at capture, chain-of-custody logging for every access, WORM storage (S3 Object Lock Compliance mode) | **1.** Audit evidence pipeline: `npx audit-evidence-pipeline --standard forensic`. **2.** Add trusted timestamp: `npx add-rfc3161-timestamp --to evidence_pipeline`. **3.** Add cryptographic hashing: `npx add-content-hashing --algorithm SHA-256 --at-capture`. **4.** Add custody logging: `npx add-chain-of-custody --log-all-access --worm-storage`. **5.** Test: `npx test-evidence-admissibility --scenario criminal_prosecution` — must pass mock cross-examination |
| `grep -cP "moderation.tool.*slow\|load.time.*second\|title.only.decision" moderator_feedback.csv` → `count > 0` AND `grep -cP "time.to.decision\|render.time\|first.contentful.paint" moderation_tool_perf.csv` → `p95 > 1000` | Moderation tool loads post context in 8.3 seconds — moderators decide based on queue titles without opening content; accuracy dropped from 82% to 61% | Tool optimized for data completeness: loads post, full comment thread, user history (50 posts), reporter history (20 reports), ML risk score (slow inference) — all before showing decision buttons. 200 reviews/day × 8.3s = 28 min of loading | Redesign: first render = reported content + decision buttons in <1 second. Lazy-load context in parallel. ML score enriches, doesn't gate. Measure "time to decision," not "tool load time" | **1.** Profile tool: `npx profile-moderation-tool --metric time-to-first-decision`. **2.** Redesign render: move context (user history, ML score) to lazy-loaded panels. **3.** Optimize: `npx optimize-moderation-tool --target-ttfd 1000ms`. **4.** A/B test: `npx ab-test-moderation-tool --metric decision-accuracy`. **5.** Set performance budget: CI fails if p95 TTFD > 1500ms |
